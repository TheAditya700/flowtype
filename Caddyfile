:{$PORT} {
    # API routes → FastAPI backend
    handle /api/* {
        reverse_proxy http://127.0.0.1:8000
    }

    # Static frontend
    handle {
        root * /srv
        try_files {path} /index.html
        file_server
    }

    log {
        output stdout
        format console
    }
}
