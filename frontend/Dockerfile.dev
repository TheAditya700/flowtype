# Development Dockerfile for frontend with hot reload
FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package.json package-lock.json ./

# Install dependencies
RUN npm ci

# Copy source (will be mounted via volume in docker-compose for hot reload)
# COPY . .

EXPOSE 5173

# Run dev server with host 0.0.0.0 to allow external access from docker-compose network
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
