localhost {
    # API routes
    handle /api/* {
        reverse_proxy backend:8000
    }
    
    # Frontend static files
    handle {
        reverse_proxy frontend:3000
    }
    
    # Logging
    log {
        output stdout
        format console
    }
}
